# Apex

### Apex Triggers
- Usage:
```Apex
trigger TriggerName on ObjectName (trigger_events) {
   code_block
}
```
- Events:
    - `before insert`
    - `before update`
    - `before delete`
    - `after insert`
    - `after update`
    - `after delete`
    - `after undelete`